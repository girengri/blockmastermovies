import { NextPage } from "next";
import { MovieLayout } from "../../components/layouts";

const MoviePage: NextPage = () => {
    return(
        <MovieLayout pageDescription={""} title={""}>

        </MovieLayout>
    )
};

export default MoviePage;
